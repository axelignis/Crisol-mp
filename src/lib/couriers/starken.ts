import type { QuoteParams, QuoteResult } from './types'

/**
 * Stub adapter Starken (Task 1). Implementación real en Task 2.
 */
export async function quoteStarken(
  _params: QuoteParams,
): Promise<QuoteResult> {
  throw new Error('STARKEN_NOT_IMPLEMENTED')
}
