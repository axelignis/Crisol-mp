---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/001_extensions.sql
  - supabase/migrations/002_users_roles.sql
  - supabase/migrations/003_config.sql
  - supabase/migrations/004_catalog.sql
  - supabase/migrations/005_commerce.sql
  - supabase/migrations/006_content_social.sql
  - supabase/migrations/007_webhook_idempotency.sql
  - supabase/migrations/008_state_machines.sql
  - supabase/migrations/009_indexes.sql
  - supabase/migrations/010_rls.sql
  - supabase/migrations/011_auth_hook.sql
  - supabase/migrations/012_seed.sql
  - supabase/config.toml
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-06
  - AUTH-06

must_haves:
  truths:
    - "All 22 tables exist in the database after migration"
    - "All monetary columns use INTEGER not NUMERIC for CLP"
    - "Artisan table has stripe_account_id, no bank transfer fields"
    - "RLS is enabled on every table with role-based policies"
    - "RLS policies use JWT user_role claim, not DB query"
    - "State machine functions reject invalid transitions with row locks"
    - "Webhook idempotency table exists with source check constraint"
    - "Custom access token hook injects user_role into JWT"
    - "Auth user insert trigger creates user + buyer/artisan profile"
    - "commission_config table has versioned rows with effective_from"
  artifacts:
    - path: "supabase/migrations/002_users_roles.sql"
      provides: "user, artisan (Stripe Connect), buyer tables + triggers"
      contains: "stripe_account_id"
    - path: "supabase/migrations/008_state_machines.sql"
      provides: "transition_product_status, transition_order_status functions"
      contains: "FOR UPDATE"
    - path: "supabase/migrations/010_rls.sql"
      provides: "RLS policies for all 22 tables"
      contains: "auth.user_role()"
    - path: "supabase/migrations/011_auth_hook.sql"
      provides: "Custom access token hook for JWT role claims"
      contains: "custom_access_token_hook"
    - path: "supabase/migrations/007_webhook_idempotency.sql"
      provides: "webhook_event table"
      contains: "webhook_event"
  key_links:
    - from: "supabase/migrations/011_auth_hook.sql"
      to: "supabase/config.toml"
      via: "auth.hook.custom_access_token config"
      pattern: "custom_access_token"
    - from: "supabase/migrations/010_rls.sql"
      to: "supabase/migrations/011_auth_hook.sql"
      via: "auth.user_role() reads JWT claim set by hook"
      pattern: "auth.user_role"
---

<objective>
Rewrite all database migrations from scratch (D-01) with the Stripe Connect model (D-02), full RLS with JWT-based role claims, state machine transition functions with row locks, webhook idempotency, and seed data with 10% initial commission (D-07, D-08).

Purpose: Establish the complete 22-entity database foundation that all subsequent phases build on. Fix existing migration inconsistencies (NUMERIC for CLP, manual bank transfer fields, profiles table reference).
Output: 12 clean migration files, updated config.toml with auth hook, working local Supabase database.
</objective>

<execution_context>
@/Users/axelignis/crisol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/axelignis/crisol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md

<interfaces>
<!-- Existing constants that migrations must align with -->
From src/lib/utils/constants.ts:
```typescript
export const PRODUCT_STATUSES = {
  DRAFT: 'draft',
  PENDING_REVIEW: 'pending_review',
  PUBLISHED: 'published',
  CHANGES_REQUESTED: 'changes_requested',
  REJECTED: 'rejected',
  SOLD: 'sold',
} as const

export const ORDER_STATUSES = {
  PENDING_PAYMENT: 'pending_payment',
  PAID: 'paid',
  IN_PREPARATION: 'in_preparation',
  SHIPPED: 'shipped',
  DELIVERED: 'delivered',
  CANCELLED: 'cancelled',
} as const

export const USER_ROLES = {
  ADMIN: 'admin',
  ARTISAN: 'artisan',
  BUYER: 'buyer',
} as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite all 12 migration files with Stripe Connect model and INTEGER CLP</name>
  <files>
    supabase/migrations/001_extensions.sql,
    supabase/migrations/002_users_roles.sql,
    supabase/migrations/003_config.sql,
    supabase/migrations/004_catalog.sql,
    supabase/migrations/005_commerce.sql,
    supabase/migrations/006_content_social.sql,
    supabase/migrations/007_webhook_idempotency.sql,
    supabase/migrations/008_state_machines.sql,
    supabase/migrations/009_indexes.sql,
    supabase/migrations/010_rls.sql,
    supabase/migrations/011_auth_hook.sql,
    supabase/migrations/012_seed.sql,
    supabase/config.toml
  </files>
  <read_first>
    supabase/migrations/001_extensions.sql,
    supabase/migrations/002_users.sql,
    supabase/migrations/003_config.sql,
    supabase/migrations/004_catalog.sql,
    supabase/migrations/005_commerce.sql,
    supabase/migrations/006_loyalty_content.sql,
    supabase/migrations/007_indexes.sql,
    supabase/migrations/008_triggers_rls.sql,
    supabase/config.toml,
    src/lib/utils/constants.ts,
    .planning/phases/01-foundation-auth/01-RESEARCH.md
  </read_first>
  <action>
Delete all 8 existing migration files in supabase/migrations/ and create 12 new ones per D-01.

**001_extensions.sql** - Same as current: enable uuid-ossp, pgcrypto, unaccent.

**002_users_roles.sql** - Rewrite per D-02 (Stripe Connect):
- `"user"` table: id (UUID PK refs auth.users), email (TEXT UNIQUE NOT NULL), full_name (TEXT), avatar_url (TEXT), role (TEXT NOT NULL DEFAULT 'buyer' CHECK IN admin/artisan/buyer), locale (TEXT NOT NULL DEFAULT 'es'), is_active (BOOLEAN DEFAULT true), created_at, updated_at. Keep same structure as current.
- `artisan` table: id (UUID PK DEFAULT gen_random_uuid()), user_id (UUID UNIQUE NOT NULL REFERENCES "user"(id) ON DELETE CASCADE), bio, photo_url, slug (TEXT UNIQUE), instagram, website, `stripe_account_id TEXT` (Stripe Connect Express account ID), is_suspended (BOOLEAN DEFAULT false), created_at, updated_at. REMOVE all bank_ fields (bank_name, bank_account_type, bank_account_number, bank_rut, bank_email) per D-02. Add `slug` column for artisan profile URLs.
- `buyer` table: same as current (id, user_id, total_points, membership_level_id without FK yet, level_since, created_at, updated_at).
- Triggers: fn_handle_new_auth_user (AFTER INSERT on auth.users creates "user" row), fn_create_buyer_profile (AFTER INSERT on "user" where role=buyer), fn_create_artisan_profile (AFTER INSERT on "user" where role=artisan). Same logic as current 008 triggers A2/A3/A4.
- fn_update_updated_at trigger function + apply to user, artisan, buyer.

**003_config.sql** - Rewrite with INTEGER for CLP:
- `membership_level`: same structure (v2 table, create now per research recommendation).
- `commission_config`: id, commission_pct (NUMERIC(5,2) -- this IS a percentage, not money), free_shipping_threshold (INTEGER NOT NULL DEFAULT 50000 -- CLP integer), points_per_purchase (INTEGER DEFAULT 1), points_to_clp_rate (INTEGER NOT NULL DEFAULT 10 -- CLP per point), effective_from (TIMESTAMPTZ DEFAULT now()), created_by (UUID refs "user"), notes (TEXT). Per D-07 this is append-only with history.
- `coupon`: code (TEXT UNIQUE), discount_type CHECK (percentage/fixed), discount_value (INTEGER for fixed CLP amounts, NUMERIC(5,2) if percentage -- use INTEGER and interpret based on type), min_order (INTEGER -- CLP), uses_limit, uses_count, expires_at, is_active, created_by, created_at.
- Add buyer FK to membership_level.
- Seed membership levels (same as current).
- NO commission_config seed here -- moved to 012_seed.sql.

**004_catalog.sql** - Rewrite with INTEGER prices:
- `category`: same as current.
- `tag`: same as current.
- `product`: change base_price from NUMERIC(10,2) to `INTEGER NOT NULL CHECK (base_price >= 0)`. Remove currency column (CLP is canonical per CLAUDE.md). Keep all other columns same.
- `product_variant`: change price_modifier from NUMERIC(10,2) to `INTEGER NOT NULL DEFAULT 0` (can be negative). Keep stock, sku, is_available same.
- `product_media`: same as current including enforce_product_media_limits trigger.
- `product_tag`: same as current.
- `commission_slot`: change price from NUMERIC(10,2) to `INTEGER NOT NULL CHECK (price > 0)`. Remove currency column.
- NO category/tag seeds here -- moved to 012_seed.sql.

**005_commerce.sql** - Rewrite with INTEGER amounts and Stripe Connect:
- `"order"`: change all monetary NUMERIC(10,2) to INTEGER: subtotal, shipping_cost, discount_amount, commission_amount, total. Add commission_pct_snapshot (NUMERIC(5,2)) to record the rate at payment time per D-07. Remove currency column (CLP canonical). Keep buyer_or_guest constraint.
- `order_item`: change unit_price, total_price from NUMERIC(10,2) to INTEGER.
- `payment`: Rewrite for Stripe Connect model. method CHECK (stripe/crypto). stripe_payment_intent_id, stripe_transfer_id (new -- for Connect transfer to artisan), coinbase_charge_id. amount (INTEGER), artisan_net (INTEGER), commission_amount (INTEGER). Remove bank_transfer from method enum. Remove manual-transfer comments.
- `artisan_payout`: Rewrite for Stripe Connect. Remove transfer_date, transfer_ref, transfer_bank (manual bank transfer fields). Add stripe_payout_id (TEXT), stripe_transfer_ids (JSONB -- array of transfer IDs in this payout). Keep period_from, period_to, gross_amount (INTEGER), commission_amount (INTEGER), net_amount (INTEGER), status (pending/paid), notes. Amounts are INTEGER.
- `shipment`: same as current. Add updated_at trigger.
- `shipping_address`: same as current.

**006_content_social.sql** - Same structure as current (v2 tables):
- loyalty_transaction, review, wishlist_item, artisan_follower, blog_post, seo_meta, notification. No monetary columns to change. Keep as-is.

**007_webhook_idempotency.sql** - New file:
```sql
CREATE TABLE webhook_event (
  id           TEXT PRIMARY KEY,
  source       TEXT NOT NULL CHECK (source IN ('stripe', 'coinbase')),
  event_type   TEXT NOT NULL,
  processed_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  payload      JSONB
);
CREATE INDEX idx_webhook_event_source ON webhook_event(source, processed_at DESC);
```

**008_state_machines.sql** - New file with two functions:
- `transition_product_status(p_product_id UUID, p_new_status TEXT, p_actor_id UUID, p_notes TEXT DEFAULT NULL) RETURNS product` -- uses SELECT FOR UPDATE row lock. Valid transitions map: draft->[pending_review], pending_review->[published, changes_requested, rejected], changes_requested->[pending_review], rejected->[draft], published->[sold]. Updates rejection_notes for changes_requested/rejected, approved_by/approved_at/published_at for published. Raises EXCEPTION with ERRCODE P0001 on invalid transition, P0002 on not found.
- `transition_order_status(p_order_id UUID, p_new_status TEXT, p_actor_id UUID) RETURNS "order"` -- uses SELECT FOR UPDATE. Valid transitions: pending_payment->[paid, cancelled], paid->[in_preparation, cancelled], in_preparation->[shipped], shipped->[delivered]. Raises same error codes.

**009_indexes.sql** - Same as current 007_indexes.sql plus:
- Add `CREATE INDEX idx_webhook_event_source ON webhook_event(source, processed_at DESC);` (if not already in 007).
- Add `CREATE INDEX idx_artisan_slug ON artisan(slug);` for new slug column.

**010_rls.sql** - Rewrite ALL RLS policies:
- Enable RLS on all 23 tables (22 + webhook_event).
- Create helper function `auth.user_role()` that reads from JWT claim (NOT from DB query):
```sql
CREATE OR REPLACE FUNCTION auth.user_role()
RETURNS text AS $$
  SELECT COALESCE(
    (auth.jwt() ->> 'user_role'),
    'buyer'
  );
$$ LANGUAGE sql STABLE;
```
- Keep `current_artisan_id()` and `current_buyer_id()` helper functions (these need DB lookup for ownership checks).
- REMOVE `current_user_role()` function entirely -- replaced by `auth.user_role()`.
- Replace ALL `current_user_role()` references with `auth.user_role()` in policies.
- `webhook_event` RLS: enable, admin-only ALL policy. No public access.
- All other policies: same logic as current 008 but with `auth.user_role()` instead of `current_user_role()`.

**011_auth_hook.sql** - New file:
```sql
CREATE OR REPLACE FUNCTION public.custom_access_token_hook(event jsonb)
RETURNS jsonb LANGUAGE plpgsql STABLE AS $$
DECLARE
  claims jsonb;
  user_role text;
BEGIN
  SELECT role INTO user_role FROM public."user"
  WHERE id = (event->>'user_id')::uuid;

  claims := event->'claims';
  IF user_role IS NOT NULL THEN
    claims := jsonb_set(claims, '{user_role}', to_jsonb(user_role));
  ELSE
    claims := jsonb_set(claims, '{user_role}', '"buyer"');
  END IF;

  event := jsonb_set(event, '{claims}', claims);
  RETURN event;
END;
$$;

GRANT USAGE ON SCHEMA public TO supabase_auth_admin;
GRANT EXECUTE ON FUNCTION public.custom_access_token_hook TO supabase_auth_admin;
REVOKE EXECUTE ON FUNCTION public.custom_access_token_hook FROM authenticated, anon, public;
GRANT ALL ON TABLE public."user" TO supabase_auth_admin;
```

**012_seed.sql** - Seed data:
- Initial commission_config: commission_pct=10 (D-08), free_shipping_threshold=50000, points_per_purchase=1, points_to_clp_rate=10, notes='Configuracion inicial del sistema'.
- Category seeds: same 5 as current (Anillos, Collares, Pendientes, Pulseras, Arte decorativo).
- Tag seeds: same materials, occasions, techniques as current.

**supabase/config.toml** - Add auth hook configuration at the end:
```toml
[auth.hook.custom_access_token]
enabled = true
uri = "pg-functions://postgres/public/custom_access_token_hook"
```
  </action>
  <verify>
    <automated>cd /Users/axelignis/crisol && ls supabase/migrations/*.sql | wc -l && grep -l "stripe_account_id" supabase/migrations/002_users_roles.sql && grep -l "INTEGER NOT NULL" supabase/migrations/004_catalog.sql && grep -l "FOR UPDATE" supabase/migrations/008_state_machines.sql && grep -l "webhook_event" supabase/migrations/007_webhook_idempotency.sql && grep -l "custom_access_token_hook" supabase/migrations/011_auth_hook.sql && grep -l "auth.user_role" supabase/migrations/010_rls.sql && grep "custom_access_token" supabase/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/ contains exactly 12 .sql files named 001_ through 012_
    - 002_users_roles.sql contains `stripe_account_id TEXT` and does NOT contain `bank_name` or `bank_account_type` or `bank_account_number`
    - 004_catalog.sql contains `base_price  INTEGER NOT NULL` (not NUMERIC)
    - 005_commerce.sql contains `commission_pct_snapshot` and `stripe_transfer_id`
    - 005_commerce.sql does NOT contain `bank_transfer` in payment method CHECK
    - 007_webhook_idempotency.sql contains `CREATE TABLE webhook_event`
    - 008_state_machines.sql contains `transition_product_status` and `transition_order_status` and `FOR UPDATE`
    - 010_rls.sql contains `auth.user_role()` function definition and does NOT contain `current_user_role()`
    - 011_auth_hook.sql contains `custom_access_token_hook`
    - 012_seed.sql contains `commission_pct` with value 10
    - supabase/config.toml contains `[auth.hook.custom_access_token]` section with `enabled = true`
    - Old migration files (008_triggers_rls.sql etc) no longer exist
  </acceptance_criteria>
  <done>12 migration files created with Stripe Connect model, INTEGER CLP, JWT-based RLS, state machines with row locks, webhook idempotency, and auth hook. config.toml updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify database starts and migrations apply</name>
  <files>supabase/migrations/012_seed.sql</files>
  <action>
Run `supabase db reset` to apply all 12 migrations from scratch. If Supabase is not running, run `supabase start` first. Verify the output shows all 12 migrations applied successfully. Then check key schema properties:
1. artisan table has stripe_account_id column
2. No NUMERIC monetary columns (except commission_pct and discount_pct which are percentages)
3. webhook_event table exists
4. transition_product_status and transition_order_status functions exist
5. custom_access_token_hook function exists
6. RLS is enabled on all tables
  </action>
  <what-built>Complete database schema rewrite: 12 migration files with Stripe Connect model, INTEGER CLP, JWT-based RLS policies, state machine functions with row locks, webhook idempotency, and custom access token auth hook.</what-built>
  <how-to-verify>
    1. Ensure Docker is running
    2. Run `supabase db reset` (or `supabase start` if not running) from project root
    3. Verify all 12 migrations apply without errors
    4. Check tables exist: `supabase db inspect --linked=false` or connect to local DB and run `\dt public.*`
    5. Verify artisan table has stripe_account_id: connect and run `\d artisan`
    6. Verify no NUMERIC monetary columns: `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema='public' AND data_type='numeric' AND column_name IN ('base_price','subtotal','total','shipping_cost','unit_price','total_price','amount','artisan_net','commission_amount','gross_amount','net_amount','discount_amount');` should return 0 rows (except commission_pct and discount_pct which are percentages, not money)
    7. If `supabase start` fails due to Docker or CLI issues, describe the error
  </how-to-verify>
  <verify>
    <automated>cd /Users/axelignis/crisol && supabase db reset 2>&1 | tail -5</automated>
  </verify>
  <done>All 12 migrations apply without errors. Local Supabase database running with correct schema.</done>
  <resume-signal>Type "approved" if migrations apply cleanly, or describe errors</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase API | All DB queries go through PostgREST with RLS enforcement |
| auth.users -> public.user | Trigger creates user row; role comes from meta_data initially |
| JWT -> RLS | Custom access token hook injects role; RLS reads it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | RLS policies | mitigate | All 23 tables have RLS enabled; role checked via JWT claim not user-editable metadata |
| T-01-02 | Tampering | State machine transitions | mitigate | SELECT FOR UPDATE row locks in transition_product_status and transition_order_status prevent concurrent race conditions |
| T-01-03 | Repudiation | Webhook processing | mitigate | webhook_event table with TEXT PK on event.id ensures idempotency; duplicate inserts fail |
| T-01-04 | Tampering | JWT role claim | mitigate | Role read from public."user" table via auth hook, not from raw_user_meta_data which users can modify |
| T-01-05 | Information Disclosure | artisan bank data | mitigate | Bank transfer fields removed entirely per D-02; stripe_account_id is not sensitive (it's a public Connect account ID) |
| T-01-06 | Elevation of Privilege | service_role key | accept | service_role bypasses RLS by design; CLAUDE.md enforces server-side only usage; not exposed via NEXT_PUBLIC_ |
</threat_model>

<verification>
- All 12 migration files exist in supabase/migrations/
- `supabase db reset` completes without errors
- All 22+ tables present in public schema
- artisan table has stripe_account_id column, no bank_ columns
- All monetary columns are INTEGER (not NUMERIC)
- RLS enabled on every table (verified via pg_class)
- auth.user_role() function exists and returns from JWT
- custom_access_token_hook function exists
- transition_product_status and transition_order_status functions exist
- webhook_event table exists
- config.toml has auth hook section
</verification>

<success_criteria>
Database starts with all migrations applied. Schema matches the 22-entity ERD with Stripe Connect model, INTEGER CLP, JWT-based RLS, state machines, and webhook idempotency. Ready for TypeScript type generation and auth implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
