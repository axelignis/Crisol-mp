---
phase: 03-commerce
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/store/cart.store.ts
  - src/hooks/use-cart.ts
  - src/hooks/use-cart-hydration.ts
  - src/components/cart/cart-sheet.tsx
  - src/components/cart/cart-item.tsx
  - src/components/cart/cart-empty.tsx
  - src/components/cart/cart-artisan-group.tsx
  - src/components/cart/cart-badge.tsx
  - src/components/cart/cart-hydration.tsx
  - src/app/[locale]/carrito/page.tsx
  - src/app/api/cart/stock-check/route.ts
  - src/components/header/header.tsx
  - tests/e2e/cart-persist.spec.ts
autonomous: true
requirements: [COMR-01, COMR-02]
tags: [cart, zustand, ui]
must_haves:
  truths:
    - "Visitante (guest o logged) agrega piezas al carrito desde el botón del header"
    - "Carrito persiste en localStorage entre refreshes (Zustand persist)"
    - "Cart sheet (slide-over) abre desde header con badge contador"
    - "Página /carrito muestra carrito completo con grupos por artesano"
    - "Carrito soporta items de múltiples artesanos en una sola orden"
    - "Stock se valida server-side al agregar (D-06 checkpoint 1)"
    - "No hay hydration mismatch (skipHydration + gate)"
  artifacts:
    - path: "src/store/cart.store.ts"
      provides: "Zustand store con persist middleware (skipHydration)"
      exports: ["useCartStore"]
      contains: "skipHydration: true"
    - path: "src/hooks/use-cart.ts"
      provides: "Selectors y actions tipadas del cart store"
    - path: "src/hooks/use-cart-hydration.ts"
      provides: "Gate SSR-safe (returns hydrated:boolean)"
    - path: "src/components/cart/cart-sheet.tsx"
      provides: "Slide-over con grupos por artesano"
    - path: "src/components/cart/cart-badge.tsx"
      provides: "Header counter (oculto si !hydrated)"
    - path: "src/app/[locale]/carrito/page.tsx"
      provides: "Página completa de carrito"
    - path: "src/app/api/cart/stock-check/route.ts"
      provides: "POST endpoint Zod-validated, retorna stock vigente por variant"
    - path: "tests/e2e/cart-persist.spec.ts"
      provides: "E2E: agregar item, refresh, sigue ahí; multi-artisan totals"
  key_links:
    - from: "src/components/cart/cart-sheet.tsx"
      to: "src/store/cart.store.ts"
      via: "useCartStore selector + useCartHydration"
      pattern: "useCartStore"
    - from: "Add-to-cart action (en pages de producto)"
      to: "/api/cart/stock-check"
      via: "fetch POST antes de useCartStore.add"
      pattern: "stock-check"
    - from: "src/components/header/header.tsx"
      to: "src/components/cart/cart-badge.tsx + cart-sheet.tsx"
      via: "render condicional"
      pattern: "CartBadge|CartSheet"
---

<objective>
Carrito persistente Zustand multi-artesano: store con persist + skipHydration, hooks SSR-safe, cart sheet (slide-over), página `/carrito`, badge en header, stock-check endpoint para checkpoint 1 de D-06. E2E test cubre persistencia y multi-artisan.

Purpose: Cumple COMR-01 (Zustand + localStorage), COMR-02 (multi-artisan), D-01..D-03, D-06 (validación stock 1/3).
Output: Carrito funcional accesible desde cualquier página, lista para alimentar checkout (Plan 04).
</objective>

<execution_context>
@$HOME/crisol/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/crisol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-commerce/03-CONTEXT.md
@.planning/phases/03-commerce/03-RESEARCH.md
@CLAUDE.md
@.planning/phases/02-catalog-discovery/02-05-SUMMARY.md
@src/store/currency.store.ts
@src/components/ui/sheet.tsx
@messages/es/checkout.json

<interfaces>
From src/types/database.types.ts:
- product_variant.Row: { id, product_id, stock, price_modifier, ... }
- product.Row: { id, slug, base_price, title, artisan_id, ... }
From src/lib/supabase/server.ts: createClient() (RSC + route handlers)
From sonner: toast.success / toast.error
From src/components/ui/sheet.tsx: Sheet, SheetContent, SheetTrigger, SheetHeader

Cart types (definir en src/types/cart.ts si no existe; si lo añades inclúyelo en files_modified):
```ts
export type CartItem = {
  productId: string
  variantId: string
  artisanId: string
  artisanName: string  // snapshot display
  title: string        // snapshot display
  unitPrice: number    // CLP integer (base_price + variant.price_modifier)
  qty: number
  imageUrl?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cart store (Zustand persist) + hooks + stock-check endpoint</name>
  <files>src/store/cart.store.ts, src/hooks/use-cart.ts, src/hooks/use-cart-hydration.ts, src/app/api/cart/stock-check/route.ts, src/types/cart.ts</files>
  <action>
**src/types/cart.ts**: Definir `CartItem` (ver interfaces). Si ya existe, extender.

**src/store/cart.store.ts** (per D-02): `create<CartState>()(persist((set,get) => ({...}), { name: 'crisol.cart.v1', storage: createJSONStorage(()=>localStorage), skipHydration: true, version: 1 }))`. Acciones: `add(item)` (merge por variantId, suma qty), `remove(variantId)`, `setQty(variantId, qty)` (qty<=0 → remove), `clear()`. Selectores derivados: `getItemsByArtisan()` retorna `Map<artisanId, CartItem[]>`, `getSubtotal()` (suma `unitPrice * qty`), `getCount()` (suma qty). Mantener mínimo: el server recalcula totales canónicos en checkout — el store es display-only.

**src/hooks/use-cart-hydration.ts**: 
```ts
'use client'
import { useEffect, useState } from 'react'
import { useCartStore } from '@/store/cart.store'
export function useCartHydration() {
  const [hydrated, setHydrated] = useState(false)
  useEffect(() => { useCartStore.persist.rehydrate().then(()=>setHydrated(true)) }, [])
  return hydrated
}
```

**src/hooks/use-cart.ts**: Wrapper de selectors con `useCartHydration` para evitar mismatch — exporta `useCart()` retornando `{ items, count, subtotal, byArtisan, add, remove, setQty, clear, hydrated }`. Si !hydrated, retorna estructuras vacías.

**src/app/api/cart/stock-check/route.ts** (D-06 checkpoint 1):
```ts
const schema = z.object({ items: z.array(z.object({ variantId: z.string().uuid(), qty: z.number().int().positive() })).min(1).max(50) })
```
POST: parse, query `product_variant` con service-role o anon (depende de RLS — usar createClient() server con service role no es necesario; SELECT en variants debería ser público para variants de productos `published`. Si RLS bloquea, agregar policy o usar admin client). Retorna `{ ok: true, items: [{variantId, available, requested, sufficient}] }` o `{ ok: false, insufficient: [...] }` con 200 (no 4xx — el cliente decide UX).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>Store rehydrata sin warning de Next.js; stock-check valida payload y retorna disponibilidad por variant; useCart() retorna `hydrated=false` en SSR y true tras mount.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Componentes UI cart (sheet, item, badge, empty, artisan group, hydration trigger) + página /carrito + integración header</name>
  <files>src/components/cart/cart-sheet.tsx, src/components/cart/cart-item.tsx, src/components/cart/cart-empty.tsx, src/components/cart/cart-artisan-group.tsx, src/components/cart/cart-badge.tsx, src/components/cart/cart-hydration.tsx, src/app/[locale]/carrito/page.tsx, src/components/header/header.tsx</files>
  <action>
**cart-hydration.tsx**: Client component vacío en su render que solo dispara `useCartHydration()`. Montar en root layout (o en header) para inicializar rehydrate global.

**cart-badge.tsx**: 'use client', usa `useCart()`. Renderiza icono + count. Si !hydrated, render sin número (evita flicker 0→N).

**cart-empty.tsx**: Estado vacío con CTA "Explorar catálogo" → `/[locale]/catalogo` (ruta de Phase 2).

**cart-item.tsx**: Card con imagen Cloudinary (next/image), título snapshot, variant label, qty stepper (+/-), precio unitario, subtotal línea. `onRemove` → `setQty(0)`. i18n via `useTranslations('cart')`.

**cart-artisan-group.tsx**: Agrupa items por `artisanId` (D-03). Header con `artisanName` y subtotal del grupo. Render lista de cart-item.

**cart-sheet.tsx** (D-01): Sheet slide-over. Trigger es children. Body: si vacío → cart-empty; sino → lista de cart-artisan-group + footer con subtotal total + botón "Ir a checkout" → `/[locale]/checkout`. Mostrar nota: "Envío e impuestos calculados en el siguiente paso."

**src/app/[locale]/carrito/page.tsx** (D-01): Server component que renderiza un client component CartPageClient con la misma estructura del sheet pero full-page (más espacio, edición detallada). Layout 2 columnas en desktop: items izquierda, resumen sticky derecha (preview de subtotal + botón checkout). Disclaimer breve: "Stock se reserva al confirmar pago."

**header.tsx**: Agregar `<CartHydration />` (una vez) + `<Sheet><SheetTrigger><CartBadge/></SheetTrigger><SheetContent><CartSheet/></SheetContent></Sheet>`. Verificar que existe en `src/components/header/`. Si la estructura es distinta, integrar en el sitio donde Phase 2 ya tenga botón placeholder. Si no hay header con el patrón asumido, ubicar el componente que renderiza nav top-level (verificar primero con grep `header` antes de modificar).

**Add-to-cart wiring**: En `src/app/[locale]/producto/[slug]/page.tsx` o el componente client que ya tiene el botón "Agregar al carrito" (Phase 2 entregó product detail con variant selector — verificar antes), wire onClick: POST `/api/cart/stock-check` con `{variantId,qty:1}`; si `sufficient` → `useCartStore.add(...)` con snapshot (title, unitPrice = base_price + price_modifier, artisanId, artisanName, image), `toast.success`. Si insuficiente → `toast.error` con stock disponible. NO modificar layout del product detail; solo el handler.

**tests/e2e/cart-persist.spec.ts** (factory en `src/test/factories/cart.ts` si no existe): 
1. Login/guest visita producto, agrega al carrito → badge muestra 1.
2. Reload página → badge sigue en 1, sheet contiene el item.
3. Visita otro producto de OTRO artesano, agrega → sheet muestra 2 grupos.
4. Subtotal = sum(unitPrice * qty) correcto.
5. Click remove → item desaparece y persiste tras reload.
  </action>
  <verify>
    <automated>pnpm test:e2e tests/e2e/cart-persist.spec.ts</automated>
  </verify>
  <done>E2E pasa; cart sheet abre desde header sin hydration warning; multi-artisan group visible; persistencia confirmada tras reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → client UI | Datos del carrito no son trusted — re-validar precios server-side en checkout |
| client → /api/cart/stock-check | Input untrusted, validar Zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | cart unitPrice en localStorage | accept | Display-only; server recalcula en payment-intent (Plan 04) — el store nunca es fuente de verdad |
| T-03-05 | DoS | /api/cart/stock-check spam | mitigate | Zod limita `items.length <= 50`; rate limit Phase 5 |
| T-03-06 | Tampering | qty negativo / 0 | mitigate | Zod `.int().positive()`; setQty(0) → remove |
</threat_model>

<verification>
- `pnpm test:e2e tests/e2e/cart-persist.spec.ts` verde
- Manual: agregar items de 2 artesanos diferentes, verificar agrupación
- DevTools: no hydration mismatch warning en consola
</verification>

<success_criteria>
- Cart sheet + /carrito accesibles desde cualquier página
- Stock-check rechaza qty inválida y reporta insuficiencias
- Persistencia entre refreshes verificada por E2E
- Multi-artisan rendering correcto
</success_criteria>

<output>
Crear `.planning/phases/03-commerce/03-02-SUMMARY.md`.
</output>
