---
phase: 03-commerce
plan: 04
type: tdd
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/lib/checkout/totals.ts
  - src/lib/checkout/totals.test.ts
  - src/lib/checkout/coupon.ts
  - src/lib/checkout/coupon.test.ts
  - src/lib/checkout/stage-cart.ts
  - src/lib/stripe/payout-ledger.ts
  - src/lib/stripe/payout-ledger.test.ts
  - src/app/api/checkout/coupon/route.ts
  - src/app/api/checkout/payment-intent/route.ts
  - src/app/api/checkout/payment-intent/route.test.ts
  - src/components/checkout/checkout-form.tsx
  - src/components/checkout/checkout-contact.tsx
  - src/components/checkout/checkout-address.tsx
  - src/components/checkout/checkout-shipping.tsx
  - src/components/checkout/checkout-coupon.tsx
  - src/components/checkout/checkout-disclaimers.tsx
  - src/components/checkout/order-summary.tsx
  - src/components/checkout/payment-stripe.tsx
  - src/app/[locale]/checkout/page.tsx
  - src/app/[locale]/checkout/confirmacion/page.tsx
autonomous: true
requirements: [COMR-02, COMR-03, COMR-04, COMR-08, COMR-09, COUP-02, COUP-03]
tags: [checkout, stripe, coupons, ui, tdd]
must_haves:
  truths:
    - "Checkout single-page con secciones: contacto → dirección → envío por artesano → cupón → pago → resumen sticky (D-18)"
    - "Disclaimers (no-returns + intl-tax) inline arriba del botón Pagar con checkbox obligatorio (D-19, COMR-03, COMR-09)"
    - "Comprador aplica cupón y ve descuento reflejado antes de confirmar (COUP-02)"
    - "Cupón valida vigencia, uses_limit, min_order, is_active (COUP-03)"
    - "Server recalcula todos los totales (subtotal, descuento, envío, comisión, total) — cliente display-only"
    - "PaymentIntent creado con apiVersion pinned, idempotencyKey deterministic, metadata.cart_snapshot_id"
    - "Guests pueden checkout con email (D-04); buyers logged requieren email verificado (D-05)"
    - "Stock revalidado al abrir checkout (D-06 checkpoint 2)"
    - "Discount aplica solo a subtotal, no al envío (D-14); plataforma absorbe (D-15) — no afecta artisan_net"
    - "Cupón se RESERVA (uses_count++) en payment-intent endpoint via UPDATE condicional (uses_count<uses_limit) ANTES de crear PI — previene race condition con uses_limit"
  artifacts:
    - path: "src/lib/checkout/totals.ts"
      provides: "computeTotals(input): canonical money math server-side"
      exports: ["computeTotals"]
    - path: "src/lib/checkout/coupon.ts"
      provides: "validateCoupon(code, subtotal): {valid, discount, reason}"
      exports: ["validateCoupon"]
    - path: "src/lib/checkout/stage-cart.ts"
      provides: "stageCart(input, totals): inserts cart_snapshot, returns id"
      exports: ["stageCart"]
    - path: "src/lib/stripe/payout-ledger.ts"
      provides: "buildPayoutLedger(orderItems, shipments, commissionPct): Array<{artisanId, gross, commission, shipping, net}> consumido por webhook"
      exports: ["buildPayoutLedger"]
    - path: "src/app/api/checkout/payment-intent/route.ts"
      provides: "POST: stage cart, create PI, return clientSecret + totals"
    - path: "src/app/api/checkout/coupon/route.ts"
      provides: "POST: validate coupon code against subtotal"
    - path: "src/app/[locale]/checkout/page.tsx"
      provides: "Single-page checkout (D-18)"
  key_links:
    - from: "src/components/checkout/payment-stripe.tsx"
      to: "/api/checkout/payment-intent"
      via: "fetch POST → clientSecret → stripe.confirmPayment"
      pattern: "clientSecret"
    - from: "src/lib/checkout/totals.ts"
      to: "src/lib/utils/commission.ts (calculateSplit) + clp.ts"
      via: "import + invoke per-artisan"
      pattern: "calculateSplit"
    - from: "src/lib/stripe/payout-ledger.ts"
      to: "src/lib/utils/commission.ts"
      via: "calculateSplit per artisan group, discount absorbed by platform"
      pattern: "calculateSplit"
---

<objective>
Checkout completo: lógica server (totals, coupon, stage-cart, payout-ledger) con TDD intensivo, endpoints API (payment-intent, coupon), y UI single-page (D-18) con secciones, Stripe Elements, disclaimers obligatorios, resumen sticky. Single-account model (D-SPLIT) — sin Stripe Connect.

Purpose: Cumple COMR-02/03/04/08/09 y COUP-02/03. Es el corazón del flujo. Webhook (Plan 05) consume `cart_snapshot` y `payout-ledger` para crear orden.
Output: Comprador completa flujo de checkout y obtiene PI clientSecret listo para confirmar pago.
</objective>

<execution_context>
@$HOME/crisol/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/crisol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-commerce/03-CONTEXT.md
@.planning/phases/03-commerce/03-RESEARCH.md
@CLAUDE.md
@.planning/phases/03-commerce/03-01-PLAN.md
@.planning/phases/03-commerce/03-02-PLAN.md
@.planning/phases/03-commerce/03-03-PLAN.md
@src/lib/utils/commission.ts
@src/lib/utils/clp.ts
@supabase/migrations/003_config.sql
@supabase/migrations/005_commerce.sql

<interfaces>
From src/lib/utils/commission.ts:
- calculateSplit(totalAmount: number, commissionPct: number): { total, commission, artisanNet, commissionPct }
From src/lib/couriers/index.ts (Plan 03):
- quoteForCart({destination, groups, preferredCourier}): Promise<{quotes:[{artisanId, quote:{source,costClp,etaDays}}]}>
From src/store/cart.store.ts (Plan 02): useCartStore (client-only — checkout endpoint recibe carrito en POST body, no lee localStorage en server)

Checkout input contract (Zod):
```ts
const checkoutSchema = z.object({
  email: z.string().email(),
  buyer_id: z.string().uuid().nullable(),
  items: z.array(z.object({ variantId: z.string().uuid(), qty: z.number().int().positive() })).min(1),
  address: z.object({
    fullName: z.string().min(2),
    line1: z.string().min(3),
    line2: z.string().optional(),
    city: z.string(),
    region: z.string(),
    countryCode: z.literal('CL'),
    postalCode: z.string().optional(),
    phone: z.string().optional()
  }),
  shipments: z.array(z.object({ artisanId: z.string().uuid(), courier: z.enum(['chilexpress','starken','flat_rate']), costClp: z.number().int().nonnegative() })).min(1),
  couponCode: z.string().optional(),
  acceptedDisclaimers: z.literal(true)
})
```

Totals output:
```ts
type Totals = {
  subtotal: number
  discount: number
  shippingTotal: number
  commission: number
  total: number
  perArtisan: Array<{ artisanId, subtotal, shipping, commission, artisanNet }>
}
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD lógica server — totals, coupon, payout-ledger</name>
  <files>src/lib/checkout/totals.ts, src/lib/checkout/totals.test.ts, src/lib/checkout/coupon.ts, src/lib/checkout/coupon.test.ts, src/lib/stripe/payout-ledger.ts, src/lib/stripe/payout-ledger.test.ts</files>
  <behavior>
**totals.ts**:
- `computeTotals({items, shipments, couponCode, productLookup, commissionPct})` retorna `Totals`
- `subtotal = sum(unitPrice * qty)` donde unitPrice viene de productLookup (canonical, no del cliente)
- `discount` calculado vía `validateCoupon`; si inválido → 0
- `shippingTotal = sum(shipments[].costClp)`
- `commission = calculateSplit(subtotal, commissionPct).commission` aplicado per-artisan (sum)
- `perArtisan` per D-15: artisan recibe gross_artisan - commission_artisan + shipping_artisan; descuento NO reduce artisan_net (la plataforma lo absorbe contra su comisión, si excede absorbe diferencia)
- `total = subtotal - discount + shippingTotal`
- Todos los valores son CLP integer; usar helpers `clp*`

**coupon.ts**:
- `validateCoupon(code, subtotal, supabase)`: read-only preview query `coupon` WHERE code AND is_active. Retorna `{valid:false, reason}` si: not found / not is_active / expires_at <= now / uses_count >= uses_limit / min_order > subtotal. Usado por `/api/checkout/coupon` (preview, no reserva).
- `reserveCoupon(code, subtotal, supabase)` (NUEVO — atomic reservation, llamado por payment-intent endpoint): conditional UPDATE para evitar race condition contra `uses_limit`:
  ```sql
  UPDATE coupon
     SET uses_count = uses_count + 1
   WHERE code = $1
     AND is_active = true
     AND (starts_at IS NULL OR starts_at <= now())
     AND (expires_at IS NULL OR expires_at > now())
     AND (uses_limit IS NULL OR uses_count < uses_limit)
     AND (min_order IS NULL OR min_order <= $2)
   RETURNING id, discount_type, discount_value
  ```
  Si retorna 0 filas → `{valid:false, reason:'race_or_limit_reached'}`. Si 1 fila → calcula `discount` y retorna `{valid:true, couponId, discount}`. Plan 05 webhook ya NO incrementa `uses_count` (la reserva se hizo aquí), solo asocia `coupon_id` a la order. TODO comment: si webhook nunca llega (PI abandonado), uses_count queda inflado — cleanup job fuera de alcance Phase 3 (defer Phase 5).
- Si percentage: `discount = clpPercentFloor(subtotal, discount_value)`
- Si fixed: `discount = min(discount_value, subtotal)` (no más que el subtotal)
- Cap descuento al subtotal (D-14: discount no toca shipping)

**payout-ledger.ts** (D-SPLIT, D-07 single-account, D-15 platform absorbs discount):
- `buildPayoutLedger({perArtisan, discount, commissionPct})`: para cada artesano retorna `{artisanId, gross, commissionGross, discountAbsorbedByCommission, shippingClp, netToArtisan, platformNet}`
- Regla D-15: descuento se aplica primero contra la comisión total (no la del artesano específico — distribuir proporcional o cargar al primer artesano? — usar proporcional al subtotal por artesano). Si discount excede sum(commission), la plataforma asume diferencia (puede quedar negativa); artisan SIEMPRE recibe `gross - commission_original + shipping` íntegro
- INVARIANTE: `sum(netToArtisan) + platformNet = subtotal - discount + shippingTotal` (= total cobrado al buyer)

Tests (al menos 12):
- totals: 1 artesano single item → math correcto
- totals: 2 artesanos, 3 items, totales correctos
- totals: variant price modifier sumado correctamente al unitPrice
- totals: coupon percentage 10% aplica a subtotal, no a shipping
- totals: coupon fixed > subtotal capa al subtotal
- coupon: not found → invalid:'not_found'
- coupon: expired → invalid:'expired'
- coupon: uses_limit reached → invalid:'limit_reached'
- coupon: min_order > subtotal → invalid:'min_order'
- coupon: percentage 10% sobre 100000 → 10000
- coupon: fixed 5000 cap si subtotal=3000 → 3000
- reserveCoupon: happy path → uses_count incrementado, retorna couponId
- reserveCoupon: uses_limit alcanzado en concurrencia (mock UPDATE retorna 0 rows) → invalid:'race_or_limit_reached', no incremento
- reserveCoupon: cupón inactivo / expirado → 0 rows → invalid
- payout-ledger: discount absorbido por commission, artisan_net intacto
- payout-ledger: discount > total commission, platform asume negativo, artisans intactos
- payout-ledger: invariant `sum(net) + platform = total` (property test)
  </behavior>
  <action>
RED→GREEN→REFACTOR. Usar `vi.mock` para Supabase en coupon test. `commissionPct` tomarlo de `commission_config` activo (en el endpoint real); en tests pasarlo como argumento. Importar `calculateSplit` de `src/lib/utils/commission.ts`. NO hardcodear `0.10`.

`stage-cart.ts` (no requiere TDD agresivo — wrapper):
```ts
export async function stageCart(supabase, input: CheckoutInput, totals: Totals): Promise<string> {
  const { data, error } = await supabase.from('cart_snapshot')
    .insert({ payload: input, totals, email: input.email, buyer_id: input.buyer_id, expires_at: new Date(Date.now()+30*60*1000).toISOString() })
    .select('id').single()
  if (error) throw error
  return data.id
}
```
  </action>
  <verify>
    <automated>pnpm test src/lib/checkout/ src/lib/stripe/payout-ledger.test.ts</automated>
  </verify>
  <done>18+ tests pasan; invariantes de ledger respetados; coupon valida 4+ rules; reserveCoupon es atómico (conditional UPDATE) y previene race vs uses_limit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API routes (coupon validate + payment-intent) + UI checkout completo</name>
  <files>src/app/api/checkout/coupon/route.ts, src/app/api/checkout/payment-intent/route.ts, src/app/api/checkout/payment-intent/route.test.ts, src/components/checkout/checkout-form.tsx, src/components/checkout/checkout-contact.tsx, src/components/checkout/checkout-address.tsx, src/components/checkout/checkout-shipping.tsx, src/components/checkout/checkout-coupon.tsx, src/components/checkout/checkout-disclaimers.tsx, src/components/checkout/order-summary.tsx, src/components/checkout/payment-stripe.tsx, src/app/[locale]/checkout/page.tsx, src/app/[locale]/checkout/confirmacion/page.tsx, src/lib/checkout/stage-cart.ts</files>
  <behavior>
**/api/checkout/coupon** POST: `{code, subtotal}` → invoke `validateCoupon` → `{valid, discount, reason?}`.

**/api/checkout/payment-intent** POST: schema `checkoutSchema` (ver interfaces). Steps:
1. Parse Zod
2. Si `buyer_id` present: verificar email_confirmed via Supabase Auth (D-05); si no → 403
3. Server-side stock revalidate (D-06 cp 2): SELECT product_variant FOR UPDATE? (no necesario — webhook lo hace; aquí solo SELECT y comparar). Si insuficiente → 409 con detalles.
4. Lookup canonical: query `product` + `product_variant` por items → construir `productLookup` con base_price, price_modifier, artisan_id, snapshot_title.
5. `commissionPct` = SELECT más reciente de commission_config (effective_from <= now ORDER BY effective_from DESC LIMIT 1).
6. Si `couponCode` presente: invocar `reserveCoupon(code, subtotal, supabase)` (atomic conditional UPDATE — D-15 race fix). Si `valid:false` → reject con 409 y reason. Si valid → guardar `couponId` y `discount` para totals. Si NO hay couponCode → `discount=0, couponId=null`.
7. `computeTotals({...,reservedDiscount, couponId})` → `Totals` (NO recomputa discount; usa el valor ya reservado). Verificar que `shipments` cubre todos los artesanos (1:1 con grupos detectados en items).
8. `stageCart` → snapshotId. `cart_snapshot.totals` incluye `couponId` para que el webhook (Plan 05) lo asocie a la order sin re-incrementar uses_count.
9. `stripe.paymentIntents.create({ amount: totals.total, currency: 'clp', automatic_payment_methods:{enabled:true}, metadata:{cart_snapshot_id: snapshotId, buyer_id: input.buyer_id ?? '' }, receipt_email: input.email }, { idempotencyKey: `pi:${snapshotId}` })`.
10. Return `{ clientSecret: pi.client_secret, totals, snapshotId }`.

NOTA D-15 (race fix): `reserveCoupon` usa UPDATE condicional con `RETURNING`. Si entre validate-preview (paso opcional desde UI) y este reserve el cupón se agotó, retorna 0 filas y el endpoint responde 409 `coupon_race_lost` — el cliente reintenta sin cupón o muestra mensaje. Si el PI nunca se confirma (buyer abandona), el `uses_count` queda inflado: TODO cleanup job (Phase 5).

**Route test (vitest)**: mock supabase + stripe; assert: invalid stock → 409, invalid coupon → still 200 con discount=0, valid happy path → returns clientSecret y stripe.paymentIntents.create called con amount integer y idempotencyKey.

**UI checkout single-page** (D-18, react-hook-form + zod resolver — ya en stack):
- `checkout-form.tsx`: orchestrator client component. Lee carrito desde `useCart()` (Plan 02). Si vacío → redirect /carrito. RHF con `checkoutSchema` resolver.
- `checkout-contact.tsx`: email input. Si user logged y verified, prefill + read-only.
- `checkout-address.tsx`: form fields. Country fixed CL (D-13). On valid submit dispara cotización (passing data to checkout-shipping).
- `checkout-shipping.tsx`: por cada `artisanGroup` muestra cotización (POST `/api/couriers/quote` debounced); user puede elegir entre chilexpress/starken (si ambos disponibles) o aceptar default. UI muestra "Envío Artesano X: $Y" y total envío. Si fallback flat_rate, mostrar nota "Tarifa estándar" (D-12).
- `checkout-coupon.tsx`: input + apply button → POST `/api/checkout/coupon`. Mostrar discount aplicado o reason de error (D-16, COUP-02).
- `checkout-disclaimers.tsx` (D-19, COMR-03, COMR-09): texto inline con i18n keys; checkbox obligatorio "He leído y acepto"; emite valor a RHF.
- `order-summary.tsx`: sticky right, muestra subtotal / discount / shipping / total (no muestra commission al buyer). Recalcula on change.
- `payment-stripe.tsx`: 'use client'. Llama `/api/checkout/payment-intent` con todo el form data + `acceptedDisclaimers:true` cuando user click "Pagar". Recibe clientSecret → wrap con `<Elements stripe={getStripe()} options={{clientSecret}}><PaymentElement/></Elements>`. Confirm con `stripe.confirmPayment({elements, confirmParams:{return_url: '/[locale]/checkout/confirmacion?snapshot=...'}})`.
- Botón Pagar disabled hasta: form válido + checkbox + shipments cotizados.

**checkout/page.tsx**: server component carga `commission_config` activo, pasa a client form. Layout 2-col desktop (form izquierda, summary derecha sticky), stack mobile.

**checkout/confirmacion/page.tsx**: interim page (verifica si webhook ya creó orden → redirect a `/pedido/{id}` con token; si no, polling/loader). Plan 06 entrega `/pedido/{id}` final; aquí solo el interim.

**stage-cart.ts**: ya descrito en task 1.
  </behavior>
  <action>
Implementar todo lo anterior. Para Stripe Elements, instalar nada (deps ya existen). Usar `@stripe/react-stripe-js` `<Elements options={{clientSecret, appearance:{theme:'stripe'}, locale:'es'}}>`.

**Route test** (`payment-intent.route.test.ts`): mock stripe + supabase; 4 cases: happy path multi-artisan, stock insufficient → 409, missing shipments → 400, country != CL → 400.

NO incluir tests E2E aquí — Plan 07 los cubre. Sí asegurar que el flujo manual end-to-end es ejercitable: levantar dev, agregar al carrito, llegar a `/checkout`, llenar form, click Pagar con tarjeta `4242...`, redirect a `/checkout/confirmacion`. Webhook (Plan 05) procesará el evento y `/pedido/{id}` (Plan 06) mostrará confirmación.
  </action>
  <verify>
    <automated>pnpm test src/app/api/checkout/payment-intent/route.test.ts && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>Flujo manual `/carrito` → `/checkout` → form completo → Pagar redirige a Stripe; clientSecret retornado; cart_snapshot insertado; PI creado con amount integer y idempotencyKey deterministic; disclaimers gating del botón verificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/checkout/* | Cart, address, coupon untrusted; Zod + server recompute |
| client → Stripe Elements | Card data nunca toca server (Stripe.js handles) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | client-submitted total/discount | mitigate | Server recomputa todo desde productLookup canónico; client values ignorados |
| T-03-11 | Spoofing | logged buyer sin email_confirmed paga | mitigate | Endpoint chequea `email_confirmed_at` (D-05); 403 si false |
| T-03-12 | Information Disclosure | exponer commission al buyer | mitigate | order_summary NO renderiza `commission`; solo subtotal/discount/shipping/total |
| T-03-13 | Tampering | coupon brute force | mitigate | Endpoint genérico error message; rate limit Phase 5 (TODO comment) |
| T-03-14 | Tampering | items[].variantId no published / sold | mitigate | productLookup query filtra `status='published'` AND `stock>0`; sino 409 |
| T-03-15 | Repudiation | disclaimer click sin record | mitigate | `acceptedDisclaimers: literal(true)` en schema; cart_snapshot.payload guarda el flag para auditoría |
| T-03-23 | Tampering (race) | coupon `uses_count > uses_limit` bajo concurrencia | mitigate | `reserveCoupon` ejecuta UPDATE condicional atómico antes de crear PI; webhook NO re-incrementa, solo asocia couponId. TODO: cleanup job para PIs abandonados (Phase 5) |
</threat_model>

<verification>
- `pnpm test src/lib/checkout/ src/lib/stripe/payout-ledger.test.ts src/app/api/checkout/payment-intent/route.test.ts` verde
- Manual: completar checkout con cupón CRISOL10 → discount 10% visible
- Manual: intentar pagar sin marcar checkbox → botón deshabilitado
- Manual: cambiar país a US en form → bloqueado o error claro
- DB: `SELECT id, totals FROM cart_snapshot ORDER BY created_at DESC LIMIT 1` muestra payload
</verification>

<success_criteria>
- Tests TDD verdes (15+)
- PI creado con amount integer CLP, currency 'clp', idempotencyKey
- Disclaimers checkbox bloquea botón
- Cupón seed CRISOL10 aplica 10%
- Multi-artisan groups visibles con shipping per group
</success_criteria>

<output>
Crear `.planning/phases/03-commerce/03-04-SUMMARY.md`.
</output>
