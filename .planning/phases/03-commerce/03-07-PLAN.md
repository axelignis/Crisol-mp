---
phase: 03-commerce
plan: 07
type: execute
wave: 6
depends_on: ["03-02", "03-04", "03-05", "03-06"]
files_modified:
  - tests/e2e/checkout-happy-path.spec.ts
  - tests/e2e/checkout-guest.spec.ts
  - tests/e2e/checkout-coupon.spec.ts
  - tests/e2e/checkout-disclaimers.spec.ts
  - tests/e2e/checkout-shipping-fallback.spec.ts
  - tests/e2e/webhook-idempotency.spec.ts
  - src/test/factories/cart.ts
  - src/test/factories/order.ts
  - src/test/factories/coupon.ts
  - src/test/utils/sign-stripe-webhook.ts
  - playwright.config.ts
autonomous: true
requirements: [COMR-01, COMR-02, COMR-03, COMR-04, COMR-05, COMR-06, COMR-07, COMR-08, COMR-09, COUP-02, COUP-03]
tags: [e2e, playwright, testing]
must_haves:
  truths:
    - "E2E happy path: agregar carrito → checkout → pagar Stripe test → orden creada y visible"
    - "E2E guest checkout: completa flujo sin auth, recibe magic-link en email mock, accede a /pedido/{id}"
    - "E2E cupón: aplicar CRISOL10, ver descuento; aplicar EXPIRADO, ver error"
    - "E2E disclaimers: botón Pagar bloqueado hasta marcar checkbox"
    - "E2E courier fallback: forzar timeout de courier, verificar flat-rate aplicada"
    - "E2E webhook idempotency: enviar mismo evento dos veces, solo una orden creada"
    - "Factories en src/test/factories/ — sin datos hardcodeados (CLAUDE.md testing rule)"
  artifacts:
    - path: "tests/e2e/checkout-happy-path.spec.ts"
      provides: "Multi-artisan happy path with Stripe test card"
    - path: "tests/e2e/checkout-guest.spec.ts"
      provides: "Guest flow + magic-link"
    - path: "tests/e2e/checkout-coupon.spec.ts"
      provides: "COUP-02 + COUP-03 verification"
    - path: "tests/e2e/checkout-disclaimers.spec.ts"
      provides: "COMR-03 + COMR-09 verification"
    - path: "tests/e2e/checkout-shipping-fallback.spec.ts"
      provides: "COMR-05 timeout fallback"
    - path: "tests/e2e/webhook-idempotency.spec.ts"
      provides: "COMR-07 idempotency verification"
    - path: "src/test/factories/cart.ts"
      provides: "Cart fixture builder"
    - path: "src/test/factories/order.ts"
      provides: "Order fixture builder"
    - path: "src/test/factories/coupon.ts"
      provides: "Coupon fixture builder"
  key_links:
    - from: "tests/e2e/*.spec.ts"
      to: "src/test/factories/*"
      via: "import + invoke en beforeEach"
      pattern: "factories"
---

<objective>
E2E Playwright suite cubriendo flujos críticos de Phase 3 con factories. Cierra requisitos de testing per CLAUDE.md ("Todo feature nuevo requiere tests E2E en Playwright antes de cerrar la rama").

Purpose: Verificar que la integración de cart + checkout + Stripe webhook + email funciona end-to-end. Gate para mergear a develop.
Output: 6 spec files verdes en `pnpm test:e2e`.
</objective>

<execution_context>
@$HOME/crisol/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/crisol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-commerce/03-CONTEXT.md
@CLAUDE.md
@.planning/phases/02-catalog-discovery/02-06-SUMMARY.md
@playwright.config.ts

<interfaces>
Stripe test cards:
- 4242 4242 4242 4242 (success)
- 4000 0000 0000 9995 (declined)
Expiry future, CVC any 3 digits, ZIP any.

Webhook testing locally:
- `stripe listen --forward-to localhost:3000/api/webhooks/stripe` (CLI install required)
- O fixtures: usar `stripe trigger payment_intent.succeeded --override payment_intent:metadata.cart_snapshot_id=...`

Factories pattern (CLAUDE.md):
- src/test/factories/cart.ts: createCartFixture() retorna {items[], totals}
- src/test/factories/coupon.ts: createCouponFixture(overrides) inserta en DB con seed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Factories + 3 specs (cart-persist ya existe; happy-path, guest, disclaimers)</name>
  <files>src/test/factories/cart.ts, src/test/factories/order.ts, src/test/factories/coupon.ts, tests/e2e/checkout-happy-path.spec.ts, tests/e2e/checkout-guest.spec.ts, tests/e2e/checkout-disclaimers.spec.ts, playwright.config.ts</files>
  <action>
**Factories** (Vitest + Playwright shared, CLAUDE.md mandate "no hardcoded data"):
- `cart.ts`: `buildCartItem({productId?, variantId?, artisanId?, qty=1, unitPrice=15000, ...})` con UUID defaults via `crypto.randomUUID()` y snapshots realistas. `buildMultiArtisanCart()` retorna 3 items de 2 artesanos.
- `order.ts`: `buildOrder({...})` para tests que necesitan filas pre-existentes (insertar via service-role client en setup).
- `coupon.ts`: `seedCoupon({code, discount_type, discount_value, ...})` upsert idempotente. Para tests, usar los seeds de migración 017 cuando aplique.

**playwright.config.ts**: revisar si tiene `webServer` config (`pnpm dev` o `pnpm build && pnpm start`). Asegurar `baseURL` = `http://localhost:3000`. Agregar projects para chromium (mínimo). Storage state per role si Phase 1 lo entregó.

**checkout-happy-path.spec.ts** (logged buyer, multi-artisan):
1. Login como buyer existente (factory de Phase 1) con email confirmado
2. Visit producto A → add to cart; visit producto B (otro artesano) → add
3. Goto `/es/carrito` → assert 2 grupos
4. Click "Ir a checkout" → `/es/checkout`
5. Fill address (factory `buildAddress()`)
6. Wait for shipping quotes (or stub responses con `page.route()` interceptando `/api/couriers/quote`)
7. No coupon
8. Marcar checkbox disclaimers
9. Click Pagar; en Stripe Elements iframe ingresar `4242 4242 4242 4242` (Playwright Stripe iframe pattern)
10. Submit; redirect a `/checkout/confirmacion` → poll → `/pedido/{id}`
11. Assert # de pedido visible, snapshot titles, total

**checkout-guest.spec.ts**: similar pero sin login. Email = `guest+${ts}@example.com`. Tras pago, mock Resend con interceptor o leer última fila `webhook_idempotency` para extraer orderId, construir token con secret de test, navegar a `/pedido/{id}?token=...` y assert visible.

**checkout-disclaimers.spec.ts** (COMR-03, COMR-09):
1. Llegar a checkout con cart válido
2. Llenar todo excepto checkbox
3. Assert texto "no devoluciones" y "impuestos internacionales" visible
4. Assert botón Pagar `disabled`
5. Click checkbox → botón habilitado
6. Click checkbox de nuevo (uncheck) → vuelve disabled
  </action>
  <verify>
    <automated>pnpm test:e2e tests/e2e/checkout-happy-path.spec.ts tests/e2e/checkout-guest.spec.ts tests/e2e/checkout-disclaimers.spec.ts</automated>
  </verify>
  <done>3 specs verdes; factories sin datos hardcoded; Stripe iframe interactuable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Specs cupón + courier fallback + webhook idempotency</name>
  <files>tests/e2e/checkout-coupon.spec.ts, tests/e2e/checkout-shipping-fallback.spec.ts, tests/e2e/webhook-idempotency.spec.ts, src/test/utils/sign-stripe-webhook.ts</files>
  <action>
**checkout-coupon.spec.ts** (COUP-02, COUP-03):
1. Cart con subtotal > 30000 (para BIENVENIDA min_order)
2. Aplicar `CRISOL10` → assert discount = 10% del subtotal visible en summary
3. Click remove cupón → discount = 0
4. Aplicar `EXPIRADO` → assert mensaje de error "expirado" y discount sigue 0
5. Aplicar `INEXISTENTE` → error "no encontrado"
6. Aplicar `BIENVENIDA` con cart < 30000 → error min_order

**checkout-shipping-fallback.spec.ts** (COMR-05):
1. Cart válido
2. `page.route('**/api/couriers/quote', route => { setTimeout(() => route.abort('timedout'), 6000) })` — fuerza timeout
3. Goto checkout, llenar address
4. Wait shipping section
5. Assert source `flat_rate` visible y nota "Tarifa estándar"
6. Verificar que el flujo NO se bloquea — botón Pagar puede habilitarse

**webhook-idempotency.spec.ts** (COMR-07):
- Strategy: usar Stripe CLI o API directamente. Como E2E full, simplificar:
  1. Hacer request directo POST a `/api/webhooks/stripe` con body fixture firmado (helper que firma con `STRIPE_WEBHOOK_SECRET` de test)
  2. Primera invocación → 200 con `orderId`; verifica orden creada en DB (cliente Supabase admin en test)
  3. Segunda invocación con mismo event.id → 200 `duplicate:true`; assert que NO se creó segunda orden (count en DB sigue siendo el mismo)
  4. Tercer caso: event con event.id distinto pero mismo cart_snapshot_id → debería fallar createOrder (snapshot ya consumido o stock 0) → 200 con error_message en webhook_idempotency
- Crear helper `src/test/utils/sign-stripe-webhook.ts` (declarado en files_modified) que use `stripe.webhooks.generateTestHeaderString({payload, secret})` para producir signature válida en tests sin depender de `stripe listen`.

**Considerar:** si `stripe listen` no está disponible en CI, los webhook tests pueden ejercitarse con HTTP directo + signature manual (Stripe SDK provee `stripe.webhooks.generateTestHeaderString({...})` para tests).
  </action>
  <verify>
    <automated>pnpm test:e2e tests/e2e/checkout-coupon.spec.ts tests/e2e/checkout-shipping-fallback.spec.ts tests/e2e/webhook-idempotency.spec.ts</automated>
  </verify>
  <done>3 specs adicionales verdes; idempotencia verificada en DB; cupón validation cubre 5+ casos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test browser → app | Tests usan datos de test; nunca tocar producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-27 | Information Disclosure | Stripe test secret en CI logs | mitigate | playwright.config.ts toma de env, .env.test.local en gitignore |
| T-03-28 | Tampering | tests dejan datos en DB local | accept | factories generan UUIDs únicos; supabase reset disponible |
</threat_model>

<verification>
- `pnpm test:e2e` suite completa verde
- `pnpm test && pnpm lint` sigue verde
- Manual review: cero datos hardcoded en specs (todos via factory)
</verification>

<success_criteria>
- 6 specs E2E pasan
- Factories cubren cart, order, coupon
- Webhook idempotency verificada con count en DB
- Cupón valida 5+ casos
</success_criteria>

<output>
Crear `.planning/phases/03-commerce/03-07-SUMMARY.md`.
</output>
